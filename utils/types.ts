export type MapType = {
    name: string;
    attribution: string;
    url: string;
    ext?: string;
    img?: string;
};

export type Map = {
    id?: string;
    name: string;
    description?: string;
    user_id?: string;
    created_at?: string;
};
